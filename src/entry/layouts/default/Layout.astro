---
// Import the global.css file here so that it is included on
// all pages through the use of the <BaseHead /> component.
import '@/styles/tailwind-default.css';
import '@/styles/global.css';

import type { Locale } from '@/i18n';

// 从路径中提取语言
const pathname = Astro.url.pathname;
const isChinese = pathname.startsWith('/zh');
const locale = (isChinese ? 'zh' : 'en') as Locale;

interface Props {
  title?: string;
  description?: string;
}

const { title = 'Sophye AI', description = 'Your intelligent AI assistant' } = Astro.props;
---

<!DOCTYPE html>
<html lang={locale}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title}</title>
    <meta name="description" content={description} />
    <link rel="stylesheet" href="/src/shared/styles/global.css" />
  </head>
  <body class="font-sans bg-white text-secondary-900">
    <main class="pt-24 pb-16">
      <slot />
    </main>
  </body>
</html>
